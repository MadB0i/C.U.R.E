[package]
name = "fake-overlay"
version = "0.1.0"
edition = "2021"
description = "CURE TEST FIXTURE ONLY - inert fullscreen topmost window used to validate cure-gui surfacing; not malware, ships no malicious behavior"

[workspace]

[[bin]]
name = "fake-overlay"
path = "src/main.rs"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_UI_WindowsAndMessaging",
] }
